<!-- verified: 2026-04-17 · Ch.10 Claude Code hands-on -->

# Hands-On · Ch.10 Claude Code

One file:

- `hooks.json` — a minimal `.claude/hooks.json` example with a
  stop-on-test-failure `PreToolUse` rule, a diff-capturing `PostToolUse`
  rule, and a `SessionEnd` cost-report trigger.

**Important.** The file is *synthesized from public documentation*
{cite}`anthropic2024claudecode`; it is **not** a copy of any internal
Claude Code file. Readers who drop it into their own `.claude/` setup are
using a reconstruction, not a leaked artefact.
